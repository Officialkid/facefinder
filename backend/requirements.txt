# FaceFinder Backend Requirements

# FastAPI runtime
fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.9

# Core dependencies
numpy==1.24.3
opencv-python==4.8.1.78
Pillow==10.1.0

# InsightFace (High accuracy, recommended)
insightface==0.7.3
onnxruntime==1.16.3  # CPU version
# onnxruntime-gpu==1.16.3  # GPU version (uncomment if using GPU)

# DeepFace (Alternative, easier to use)
deepface==0.0.79
tensorflow==2.14.0  # Required by DeepFace
# tensorflow-gpu==2.14.0  # GPU version (uncomment if using GPU)

# Utilities
tqdm==4.66.1
requests==2.31.0
gdown==5.2.0

# Optional: For better performance
# scikit-learn==1.3.2
# faiss-cpu==1.7.4  # For fast similarity search
# faiss-gpu==1.7.4  # GPU version
